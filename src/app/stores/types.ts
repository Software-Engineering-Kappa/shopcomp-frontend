export type Chain = {
  id: number
  name: string
}

export type Store = {
    id: number
    chainId: number
    houseNumber: string
    street: string
    city: string
    state: string
    postCode: string
    country: string
}